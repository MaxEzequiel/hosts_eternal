Bypass method that opens host drivers to continuously block web pages, 
opens the path if it exists and eliminates all content to add a new one. 
If the file does not exist, create it again and insert the corresponding 
content to block websites, the file will continue to be created even 
if it is renamed, deleted or its content is changed
